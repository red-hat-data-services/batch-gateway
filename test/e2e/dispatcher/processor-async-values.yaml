processor:
  config:
    dispatchMode: async
    asyncDispatch:
      resultPollTimeout: "30s"
    modelGateways:
      sim-model:
        inferencePoolName: "sim-pool"
      sim-model-inject:
        inferencePoolName: "sim-pool-inject"
